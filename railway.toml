# Force Railway to build the Python bot via Dockerfile.
# The Mini App (index.html) is hosted separately on GitHub Pages.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
